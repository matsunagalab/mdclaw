{
  "task_id": "P22_prep_forcefield_water_fidelity",
  "public_source": "PDB 2LZM",
  "capability_tags_extra": [
    "forcefield_selection",
    "water_model"
  ],
  "task_intent": "Force-field/water model fidelity: honor a supported user-specified force-field/water pair, such as ff19SB with OPC, rather than silently falling back to defaults.",
  "references": [
    {
      "source": "PDB 2LZM",
      "url": "https://www.rcsb.org/structure/2LZM"
    }
  ],
  "scoring": {
    "deterministic_checks": [
      {
        "check_id": "explicit_solvent_rescanned",
        "check_type": "solvent_regime_rescan",
        "min_water_residues": 1,
        "required_solvent_regime": "explicit",
        "topology_manifest_path": "outputs.topology",
        "weight": 0.3
      },
      {
        "check_id": "water_model_fingerprint_matches_opc",
        "check_type": "water_model_fingerprint",
        "required_water_model": "OPC",
        "topology_manifest_path": "outputs.topology",
        "weight": 0.7
      },
      {
        "$bundle": "topology_minimization"
      }
    ]
  }
}
