# Benchmark Evaluation Workflow (moved)

The full evaluation workflow reference moved with MDPrepBench to
<https://github.com/matsunagalab/MDPrepBench> (`docs/evaluation-workflow.md`).
For MDStudyBench, the run/score lifecycle is documented in `mdstudybench.md`;
the same `run_benchmark_agent` automation applies with
`--dataset-dir benchmarks/mdstudybench`.
