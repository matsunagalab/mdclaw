# MDAgentBench Suite Design (moved)

The suite design document moved with MDPrepBench to
<https://github.com/matsunagalab/MDPrepBench> (`docs/suite_design.md`), which
holds the preparation-suite design and the shared design position. The
study-suite contract is documented here in `mdstudybench.md`.
