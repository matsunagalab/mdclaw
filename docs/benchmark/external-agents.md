# Evaluating External Agents (moved)

The external-agent onboarding guide moved with MDPrepBench to
<https://github.com/matsunagalab/MDPrepBench> (`docs/external-agents.md`).
For MDStudyBench, an external agent is plugged in the same way — through
`run_benchmark_agent --agent-command` — and the S01 contract it must follow is
described in `mdstudybench.md`.
