name: mdclaw
channels:
  - conda-forge
dependencies:
  - python=3.12
  # --- conda-only or conda-preferred ---
  - ambertools>=24.0
  - pdbfixer>=1.9
  - rdkit>=2023.9.1
  - openblas
  - matplotlib
  # --- pip dependencies (delegated to pyproject.toml) ---
  - pip
  - pip:
    - -e .
    - git+https://github.com/matsunagalab/FASPR
